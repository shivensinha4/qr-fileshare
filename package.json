{
  "name": "qr-fileshare",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-dev": "cd client && yarn start -o",
    "server-dev": "nodemon index.js --ignore client/ --ignore config.json",
    "dev": "concurrently --kill-others-on-fail \"yarn client-dev\" \"yarn server-dev\"",
    "client-build": "cd client && yarn build",
    "build-start": "cd client && yarn build && cd .. && node index.js",
    "start": "node index.js"
  },
  "author": "Shiven Sinha",
  "license": "ISC",
  "dependencies": {
    "console-png": "^1.2.1",
    "express": "^4.16.3",
    "multer": "^1.3.0",
    "node-persist": "^3.0.0",
    "opn": "^5.3.0",
    "qr-image": "^3.2.0",
    "socket.io": "^2.1.0",
    "socket.io-client": "^2.1.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
